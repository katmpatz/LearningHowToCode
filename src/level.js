export default class Level {
    constructor() {
        //id
        //istructions
        //blocksAvailable
        //startPosition = [x,y]
        //checkPoints = [(x,y), (x,y)]
        //planetList = [(x,y, filename),(x,y, filename)]
    }
}